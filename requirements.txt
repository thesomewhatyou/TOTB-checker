discord.py>=2.0.0 # For Discord bot functionality
aiohttp>=3.8.0    # For asynchronous HTTP requests
beautifulsoup4>=4.9.0 # For HTML parsing
python-dotenv>=0.19.0 # For loading .env files
trafilatura>=1.0.0  # For advanced web scraping/content extraction (though primary parsing is BS4)

# Specific versions can be pinned further if needed, e.g., discord.py==2.3.2
# For now, using minimum compatible versions.
